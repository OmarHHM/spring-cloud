server:
  port: 8081

spring:
  profiles:
    active: composite
  cloud:
    config:
      server:   
        git:
          uri: https://github.com/OmarHHM/spring-cloud.git
          searchPath: config
          username: ${GIT_USER}
          password: ${GIT_PWD}
            
  security:
    user:
      name: 0II4r
      password: I045301210
