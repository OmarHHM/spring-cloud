server:
  port: 8081



spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/OmarHHM/spring-cloud.git
          searchPaths: config
          username: ${GIT_USER}
          password: ${GIT_PWD}
          default-label: "master"
  security:
    user:
      name: 0II4r
      password: I045301210
